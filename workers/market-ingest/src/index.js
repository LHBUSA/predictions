import { fail, ok } from '../../_shared/contract.js'; import { KalshiPublicAdapter } from '../../../src/kalshi.js'; export default {async fetch(request){const url=new URL(request.url);if(url.pathname!='/capture')return fail('NOT_FOUND','Use /capture',404);const adapter=new KalshiPublicAdapter();const page=await adapter.listMarkets({status:url.searchParams.get('status')||'open',limit:Number(url.searchParams.get('limit')||100)});return ok(page,{venue:'kalshi'});}};
