import { fail, ok } from '../../_shared/contract.js'; export default {async fetch(request){if(request.method!=='POST')return fail('METHOD_NOT_ALLOWED','POST required',405);const body=await request.json();return ok({provider:body.provider||null,sourceId:body.sourceId||null,capturedAt:new Date().toISOString(),status:'scaffold',note:'source adapters plug in here; point-in-time retention is mandatory'});}};
