import { fail, ok, requireFields } from '../../_shared/contract.js'; export default {async fetch(request){if(request.method!=='POST')return fail('METHOD_NOT_ALLOWED','POST required',405);const body=await request.json();requireFields(body,['eventId','resolutionAuthority']);return ok({eventId:body.eventId,resolutionAuthority:body.resolutionAuthority,status:'pending',checkedAt:new Date().toISOString()});}};
