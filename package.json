{
  "name": "propbetedge-predictions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prediction-market intelligence foundation for PropBetEdge Predictions.",
  "scripts": {
    "test": "node --test",
    "check": "find src workers -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "engines": {
    "node": ">=20"
  }
}
